{% extends 'base.html.twig' %}
{% block body %}

<div class="row row-cols-1 row-cols-md-3 g-4">

	{% for laville in hackathon %}


		<div class="col">
			<div class="card" style="width: 35rem;">
				<img src="{{laville.image}}" class="card-img-top">
				<div class="card-body">
					<h5 class="card-title">{{laville.theme}}</h5>
                    
				</div>
			</div>
		</div>
	{% endfor %}
</div></div>{% endblock %}
