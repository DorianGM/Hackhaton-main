{% extends 'base.html.twig' %}
{% block body %}

<div class="row row-cols-1 row-cols-md-3 g-4">

	{% for laville in hackathons %}


		<div class="col">
			<div class="card" style="width: 35rem;">
				<img src="{{laville.image}}" class="card-img-top">
				<div class="card-body">
					<h5 class="card-title">{{laville.theme}}</h5>
                    <a href="{{path('details',{'id':laville.idH})}}" class="btn btn-outline-success">Détails</a>
					{% if laville.nbPlaces > 0 %}
						<a href="#" class="btn btn-outline-info">S'inscrire</a>
					{% endif %}
				</div>
				</div>
			</div>
		</div>
	{% endfor %}
</div></div>{% endblock %}
